{
  "name": "popo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "wrangler pages deploy dist",
    "deploy:dev": "wrangler pages deploy dist --env development",
    "preview:wrangler": "wrangler pages dev dist",
    "db:migrate": "wrangler d1 execute your-database-id --local --file=schema.sql",
    "db:migrate:remote": "wrangler d1 execute your-database-id --remote --file=schema.sql"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "vue": "^3.5.24",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "esbuild": "^0.25.12",
    "vite": "npm:rolldown-vite@7.2.5"
  },
  "overrides": {
    "vite": "npm:rolldown-vite@7.2.5"
  }
}
